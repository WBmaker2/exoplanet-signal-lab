# 07. 외계행성 신호 수사대

- 프로젝트: `exoplanet-signal-lab`
- 작성일: 2026-09-15 / 상태: 구현 전 설계 / 우선순위: 7
- 대상: 중·고등학교 지구과학·물리·통계 / 활동: 30분
- [공통 원칙](./00-shared-design-principles.md) · [목록](./README.md)

## 1. 질문·차별성

‘별빛이 어두워졌다는 사실만으로 행성이 있다고 말할 수 있을까?’ 밝기 시계열에 맞는 후보 모형을 만들고 추가 관측을 계획한다.
기존 태양계 앱의 알려진 천체 탐색과 달리 관측 신호에서 보이지 않는 대상의 존재와 조건을 추정한다.
7순위는 잡음·별 자체 변동 등 해석의 한계를 다루어야 해 기초 3D 앱보다 검수가 많기 때문이다.

## 2. 목표·오개념

- 통과 깊이·반복 주기와 행성 모형의 관계를 설명한다.
- 관측 잡음과 신호를 구분하고 여러 후보를 비교한다.
- 한 번의 밝기 감소만으로 행성 발견을 확정하지 않는다.
- 행성 상상도를 실제 관측 사진으로 오인하지 않는다.
- 중학교는 주기·그래프 읽기, 고등학교는 모델 적합과 불확실성을 추가한다.

## 3. 흐름

1. 3분: 별빛 곡선 3개 중 반복되는 감소 후보를 고른다.
2. 7분: 반지름 비율과 주기를 조절해 가상 관측에 맞춘다.
3. 5분: 같은 후보를 3D에서 관찰하고 통과하지 않는 경사를 비교한다.
4. 10분: 제한된 시간 창 중 다음 관측을 선택해 후보를 구분한다.
5. 5분: ‘행성 후보’와 ‘추가 검증 필요’를 구분해 보고한다.
- 예제: 반지름 비율 0.1인 중심 통과에서 이상적 최대 감소 약 1%를 예측한다.

## 4. 화면·상태

| 화면 | 정보 | 행동 |
|---|---|---|
| 관측 | 시간·정규화 밝기·오차 | 구간 선택 |
| 후보 | 반지름 비·주기·경사 | 모형 조절 |
| 비교 | 관측·모형·잔차 | 후보 저장 |
| 일정 | 가능한 관측 창 | 예산 내 선택 |
| 보고 | 근거·대안·한계 | 결론 기록 |

loading → exploring → fitting → scheduled → comparing. 실시간 다운로드 없이 검수된 고정 자료로 시작한다.
자료가 부족하면 적합 실패가 아닌 ‘구분할 정보 부족’을 표시한다. 후보 변경은 관측 자료를 바꾸지 않는다.

## 5. 모델·단위

- P0는 균일한 밝기의 구형 별, 불투명 구형 행성, 원궤도, 단일 행성, 별 질량 1 태양질량·반지름 1 태양반지름이다.
- 주기 P∈[2,20]일, 반지름 비 k=Rp/Rs∈[0.03,0.2], 기울기 i∈[85°,90°].
- a=(G Mstar P²/(4π²))^(1/3). 내부 계산은 SI, 화면은 일·별 반지름 단위로 변환한다.
- 위상 φ=2π(t-t0)/P. 투영 거리 d=a√(sin²φ+cos²i cos²φ).
- cosφ>0인 앞쪽 반궤도에서만 별을 가린다. 뒤쪽 위치에서 같은 감소가 생기지 않도록 한다.
- F(t)=1-A_overlap(d,Rs,Rp)/(πRs²). 원 겹침 면적은 분리·포함·부분 겹침 세 경우로 계산한다.
- 중심 완전 통과 구간에서 F=1-k². 접촉 경계에서는 F가 연속이어야 한다.
- 잡음은 정규화 밝기 σ=0.001~0.005 후보, seed 고정. 관측 시간이 불규칙해도 실제 t로 계산한다.
- 별 가장자리 어두워짐·편심·다중 행성·항성 흑점은 P0 물리 적합에서 제외하며 대안 설명으로만 소개한다.
- 적합 지표 χ²=Σ((Fobs-Fmodel)/σ)². σ=0 데이터에는 RMSE를 사용한다.
- 작은 잔차가 행성 존재의 증명은 아니다. 후보 모형과 자료의 일치도라고 표시한다.

## 6. 데이터·평가

- Star: massKg, radiusM, brightnessModel.
- Candidate: periodDays, radiusRatio, inclinationDeg, transitEpochDays.
- Observation: timesDays, fluxes, uncertainties, sourceType, seed, modelVersion.
- Window: startDay, durationDay, cost. 선택 예산은 시간 창 3개로 시작한다.
- 평가: 반복 신호 확인, 조건과 곡선 연결, 추가 관측 이유, 결론의 불확실성.
- 정답 파라미터 하나만 요구하지 않는다. 허용 잔차를 만족하는 후보와 관측상 구분 불가한 후보를 함께 인정한다.

## 7. 시각화·3D

Three.js는 별·행성·관측 방향의 관계를 보여준다. 거리와 반지름을 시각상 확대하면 확대 여부를 표시한다.
그래프는 실제 모델 계산을 사용하며 3D 화면의 픽셀 면적으로 밝기를 계산하지 않는다.
2D 궤도·원 겹침 도식과 표로 대체한다. 시간 슬라이더와 그래프 커서는 같은 t를 공유한다.

## 8. 이미지

- P0 미션 배경 3장+가상 행성 상상도 6장=9장. 확장 24장.
- 예시: ‘가상의 외계행성 상상도, 표면을 과학적으로 확인한 사진처럼 보이지 않는 교육용 삽화, 부드러운 색감, 문자 없음’.
- 상상도·가상 데이터·실제 관측을 명시적으로 구분한다. 밝기 곡선은 생성하지 않는다.
- 검수: 행성 크기나 색이 숨은 정답을 알려주는 단서가 되지 않게 한다.

## 9. 범위·단계

- P0: 합성 자료 3종, 원궤도, 후보 비교, 관측 예산, 결론 기록.
- P1: 검수한 실제 광도곡선, 주변부 감광, 자료 공백. 실제 자료의 이용 조건·전처리를 먼저 기록한다.
- 제외: 실제 발견 주장, 자동 논문 결론, 관측 장비 예약, 실시간 천체 검색.
- M1: 겹침·궤도 엔진 → M2: 합성 자료와 노이즈 → M3: 그래프·후보 → M4: 3D·미션 → M5: 모형 한계 검토.
- 모듈: orbit.ts, overlap.ts, lightCurve.ts, fitMetrics.ts, observationWindows.ts, OrbitScene.tsx, CurveView.tsx. 각각 500줄 미만.

## 10. 검증·완료

- k=0.1, 중심 통과 완전 겹침에서 F=0.99. d≥Rs+Rp면 F=1.
- 행성이 뒤쪽이면 감소 없음. φ와 φ+2π는 같은 모형값이다.
- 접촉 안팎에서 연속성, arccos 입력 반올림 범위, 반지름 0 경계를 검증한다.
- seed를 바꾸지 않은 재실행은 같은 관측. 후보 변경으로 관측 원자료가 바뀌지 않아야 한다.
- 그래프·3D 시간 일치, 키보드 시간 입력, 320px 표·곡선, reduced-motion 검증.
- gi-pulse는 관측 선택/보고 버튼, 업데이트 내역 제공, VoiceOver 제외.
- 완료: 합성 3개 미션에서 후보·관측·잔차 구분, 관측 예산, 보류 결론이 모두 작동한다.

## 11. 참고

[NASA Exoplanet Watch 자료](https://science.nasa.gov/citizen-science/exoplanet-watch/exoplanet-watch-resources/)는 앞선 아이디어 조사에서 확인한 학습 자료 링크다.
실제 자료 도입 시 데이터셋 버전과 이용 조건을 별도로 확인한다.
