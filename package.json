{
  "name": "exoplanet-signal-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "07. 외계행성 신호 수사대 — 밝기 시계열로 행성 후보를 추정하는 교실 실험실 (P0)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
