# 배포 점검 기록 — exoplanet-signal-lab P0

목적: 배포 직전까지 개선을 반복하고, 모든 판단·측정·수정을 날짜와 함께 남긴다.
기준 문서: [공통 원칙](./00-shared-design-principles.md) §6, [07 설계](./07-exoplanet-signal-lab.md) §10.

## 결정

### 2026-09-17 — 녹색(Quiet Leaf)은 3차 wash 유지, secondary 승격 안 함
- 근거: 역할당 1색 규율(파랑=모형, 빨강=결론, 황토=잔차·주의). 녹색은 저장 완료·예산 소진 같은 드문 확인에만 등장하므로 secondary로 올리면 강조가 3파전이 되어 결론 빨강의 희소성이 깨진다.
- DESIGN.md 현행이 이미 tertiary wash로 문서화되어 있어 코드·토큰 변경 없음.
- 재검토 조건: 후보마다 합격 배지를 다는 등 성공 신호가 반복 노출되는 구조가 되면 승격 재논의. 그 전에는 wash + 굵은 확인 문구로 강도 보완.

## 배포

### 2026-09-17 — GitHub 공개 저장소 + Pages 배포
- 저장소: https://github.com/WBmaker2/exoplanet-signal-lab (`main` 소스, `gh-pages` 빌드 배포)
- 공개 URL: https://wbmaker2.github.io/exoplanet-signal-lab/
- §6.6 하위 경로 검증: 루트 200, `<title>외계행성 신호 수사대</title>`, JS·CSS 상대경로(`./assets/…`) 200 확인.
- 실기기 100ms·자산 경로 최종 확인은 배포 후 실측으로 이월 (R2 기록 참조).

## 이미지 자산 (P0: 미션 배경 3장 + 상상도 6장 = 9장)

### 2026-09-18 — 착수 진단: image 2.5 무료 할당량 0, 결제 필요
- 사용자 지정 모델 image 2.5 = `gemini-2.5-flash-image`로 확인, 프롬프트 9종 작성(`tools/asset-prompts.json`), 파이프라인 준비(`tools/generate_assets.py`, 재시도 1회·연속 3회 실패 시 중단).
- 실행 결과: `429 RESOURCE_EXHAUSTED` — 무료 티어 이미지 요청 할당량 0. 이미지 모델 3종(`gemini-2.5-flash-image`, `gemini-3.1-flash-image`, `gemini-3-pro-image-preview`) 모두 동일. 텍스트 모델 키는 정상.
- 가격 기록(공식 가격 페이지 2026-09-18 열람): image 2.5 표준 $0.039/장, 배치 $0.0195/장 → 9장 ≈ $0.35(배치 ≈ $0.18). 무료 API 티어 없음. 생성 이미지에 SynthID 워터마크 포함.
- 원칙 §5에 따라 **다른 모델로 조용히 대체하지 않고** 사용자 협의로 이월. 선택지: (1) 결제 연결 후 image 2.5로 생성, (2) AI Studio 웹에서 직접 생성 후 PNG 전달, (3) 코드 절차 렌더로 대체(승인 필요), (4) P1로 이월.
- 결정: **(2) AI Studio에서 직접 생성** (2026-09-18). 사용자가 image 2.5로 생성해 `assets/raw/`에 넣기로 함. 준비물: `assets/생성-가이드.md`(붙여넣기용 프롬프트 9종 + 설정 + 검수 기준), `tools/compress_assets.py`(압축·기록), 앱 슬롯(자산 없으면 자동 숨김).

### 2026-09-18 — AI Studio 생성 진행 중 (Nano Banana 2 Lite, 사용자 승인)
- 사용자가 직접 실행하니 생성이 되어, 지시 모델로 Nano Banana 2 Lite(`gemini-3.1-flash-lite-image`)를 승인받아 진행. `tools/asset-prompts.json`의 model을 `v2-2lite`로 갱신.
- 미션 1 배너(사용자 생성, 1376×768) 저장 → 스타일 기준 통과(부드러운 교육용 삽화, 문자 없음, 사진 느낌 없음, 정답 단서 없음).
- 미션 2·3 배너 생성·저장 완료(기준 참조 첨부, 16:9, 계열 일관).
- 행성 1에서 2회 연속 내부 오류 → 속도 제한(무료 할당 소진) 의심. 잠시 대기 후 재시도 예정.
- 사용자 요청으로 AI Studio 웹 Playground에서 직접 생성 시도(브라우저 자동화, 사용자 로그인 세션, 다른 설정은 손대지 않음).
- 지시 모델 Nano Banana 2(`gemini-3.1-flash-image`, Paid 표시 $0.0672/장)로 16:9·프롬프트 입력 후 2회 실행 → 모두 "An internal error has occurred."
- 원인 검토용 3번째 시도: Nano Banana 2 Lite(`gemini-3.1-flash-lite-image`, Paid 표시 없음) → 동일 오류.
- 판정: 이 계정에는 이미지 생성 사용량이 없음(API 무료 할당량 0과 일치). Playground 실행에는 Google AI 요금제 또는 결제 연결 API 키가 필요.
- 원칙 §5에 따라 유사 시도 3회로 중단, 사용자 협의로 이월. 선택지: (a) 요금제 업그레이드 후 나노바나나2, (b) 결제 연결 API 키 연결 후 나노바나나2(장당 $0.0672 과금), (c) 코드 절차 렌더 대체(승인 필요), (d) P1 이월.

### 2026-09-18 — Nano Banana 2 Lite로 진행 (사용자 승인·직접 동작 확인)
- 사용자가 직접 실행하니 생성이 되어, 지시 모델을 Nano Banana 2 Lite(`gemini-3.1-flash-lite-image`)로 승인받아 진행. `tools/asset-prompts.json`을 model `v2-2lite`로 갱신.
- 미션 1 배너(사용자 생성, 1376×768) 저장 → 스타일 기준 통과. 미션 2·3 배너 생성·저장 완료(기준 참조 첨부, 16:9, 계열 일관, 육안 검수 통과).
- 행성 6장은 생성 5회 연속 실패(비율·참조 유무와 무관) → 무료 할당 소진으로 판정, 중단. 미션 배너 3장만 압축(WebP 11–13KB, 예산 내)·기록(reviewed)·앱 삽입 후 배포. 갤러리는 자산이 없어 자동 숨김 유지.
- 재개 조건: 할당량 리셋 후 사용자가 "계속"이라고 하면 행성 6장부터 이어서 생성.

### R3 — 2026-09-18 (자산 파이프라인 검증 · 유형 램프 정리)
- 자산 파이프라인을 임시 자리표시자 PNG로 끝까지 검증: 배너 1024×576→WebP 2KB, 행성 768×768→WebP 7KB, 기록(`assets-manifest.json`) 생성, 앱 DOM에서 배너 1장·갤러리 카드 1장·alt·태그·이미지 로드(200, image/webp) 확인. 검증 후 자리표시자·기록 삭제, 자산이 없을 때 배너 0장·갤러리 섹션 미표시·오류 0 확인.
- 접근성 수정: 미션 카드 배너 태그를 `aria-hidden`으로 두어 카드 이름이 제목만 유지되게 함(이미지는 alt="" 장식).
- 감지기 정리: `detect` 지적 10건 중 9건을 실제 개선으로 해소 — `--ink-deep` 색 토큰 추가(토큰바 하드 엣지·버튼 호버), 소형 활자 3단(0.68/0.7/0.78/0.8/0.95rem)을 문서 램프(0.85/0.75/1rem)로 통합, DESIGN.md에 `ink-deep`·`micro` 토큰과 계층 항목 추가, design.json에 colorMeta/typographyMeta/신규 컴포넌트 2종(Mission Banner, Imagination Gallery Card) 반영. 남은 1건은 범례 선见本 오탐.
- 320px 재확인: 넘침 0. 콘솔에 브라우저 자체 경고 1건(ResizeObserver loop completed)이 남으나 예외가 아니고 기능 영향 없음 — 기록만 남김.

## 검증 라운드
### R1 — 2026-09-17 (초기 구현 완료 직후)
- vitest 11/11, tsc·build 정상.
- 데스크톱(1440) + 모바일(390) 일괄 촬영, 전 흐름 스모크 테스트 무오류.
- `detect --json` 경고 1건 = 오탐(범례 선见本).
- 수정: 3D 리사이즈 후 다시 그리기, y축 제목 겹침, 그리드 늘어짐, 별 와이어프레임 제거, ResizeObserver 루프 경고 제거.
- finish 리뷰 판정: ship.
- DESIGN.md + design.json 작성.

### R2 — 2026-09-17 (배포 전: 320px·모션축소·키보드·2D대체·상대경로·성능)

- 320px 가로 넘침 발견·수정: 토큰바 브랜드 `nowrap` 부제목이 331px로 밀어냄 → 560px 이하에서 줄바꿈. 슬라이더 플렉스 최소폭(`.row > * {min-width:0}`)도 함께 보강. 수정 후 `scrollWidth=320=clientWidth` 확인.
- reduced-motion: `#start-btn` 계산 스타일 `animation:none` + 3px 정적 테두리 확인.
- 키보드: 첫 Tab 포커스가 ‘본문으로 건너뛰기’ 링크. 모든 조작이 네이티브 버튼·입력·슬라이더(방향키 지원)라 추가 포커스 함정 없음.
- WebGL 차단 시 2D 대체: `.orbit-wrap`에 SVG 1개·WebGL 캔버스 0개 + 안내 문구(“3D를 표시할 수 없어…”) 확인. 곡선 표·수치표는 그대로 동작.
- dist 상대경로: `src="./assets/…"`, `href="./assets/…"` — 하위 경로 배포 안전.
- 성능(측정 rig = headless ego-Chromium, GPU 없음): DCL 59ms·load 64ms·JS 전송 134,463B·번들 JS 510KB/CSS 8.2KB.
- 입력 반응: rig에서 슬라이더→페인트 약 1.1–2.0초가 나왔으나, Three.js 없는 미션 카드 클릭도 동일한 패턴 → 코드 경로가 아니라 소프트웨어 합성 환경 문제로 판정. 근거: 엔진 11개 테스트 총 3ms, DCL 59ms. 구조적 완화는 적용됨(요청 시 렌더·루프 없음·rAF 묶음·MSAA 제거·DPR 1.5 상한). 100ms 예산 준수는 **실기기(교실 PC·휴대폰) 측정으로 이월**하며, 이 rig 수치로 준수 주장을 만들지 않는다.
- 스크린샷 파이프라인이 세션 후반부터 CDP 타임아웃으로 중단됨(DOM 평가는 정상). R2 시각 증거는 DOM 수치로 대체하고, 320px·2D대체 화면은 배포 시 실기기로 재확인한다.
- ship 판정 이후 변경분(토큰바 줄바꿈·슬라이더 최소폭·MSAA 제거·DPR 상한·rAF 묶음)은 모두 기계적·국소적이며 vitest·tsc·build·DOM 재검증을 통과했다.
